apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-provider.fullname" . }}
  labels:
    {{- include "auth-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auth-provider.selectorLabels" . | nindent 4 }}
